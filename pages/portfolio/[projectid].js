import { useRouter } from "next/router"

const ProjectPage = () => {
    const router = useRouter()
    // console.log(router.pathname)
    // console.log(router.query)
  return (
    <div>this is projectid</div>
  )
}

export default ProjectPage